# T3 — Wave 2 Pass-1 Senior Opus Diff-Review

Endpoint: 100.92.54.25:18801/anthropic, claude-opus-4-8

I'll review this diff carefully against the contract, looking for data-destruction, correctness bugs in the real path, and idempotency issues.

Let me analyze the key areas:

**sqlite-vec dimension mismatch bug**: `CREATE VIRTUAL TABLE IF NOT EXISTS bookmark_vec USING vec0(embedding float[${dimensions}])` — created on first upsert with first vector's dimension. If a later re-embed uses a different model/dimension, the `IF NOT EXISTS` keeps the old table and inserts fail → demotes to fallback silently. Real concern but handled by demotion.

**The real-path search bug**: `searchBruteForce` reads ALL rows from `bookmark_embeddings` regardless of model. If multiple models coexist, vectors of different dimensions get compared — cosineSimilarity uses `Math.min(a.length, b.length)` masking the mismatch and producing garbage scores. This is a correctness bug in the real path (not the single-model fixture).

**sqlite-vec demotion mid-write corruption**: If `tx()` fails after writing embeddings table but the failure is in sqlite-vec, the catch re-runs the embeddings upsert (idempotent, fine). But the sqlite-vec table now has a partial set — on demotion, search uses brute-force reading embeddings table which is complete. OK.

**Idempotency of re-run**: `embed.ts` default skips already-embedded (WHERE NOT EXISTS by model). `--force` re-embeds; upsert is ON CONFLICT idempotent. sqlite-vec upsert does DELETE+INSERT by rowid, rowid stable via UNIQUE bookmark_id. Re-run safe. Good.

**FTS rebuild**: `rebuildSearchFts` does `DELETE FROM bookmark_search_fts` then re-inserts every search by default (`rebuildFts ?? true`). This is wasteful but on a derived table it owns — not destructive to other tiers. However it runs on EVERY search query, full-table rebuild per query = performance issue, and writes to DB on every read-path search (could block/contend). Notable.

**Unbounded backfill guard**: Present and correct (`--limit` or `--confirm-full-backfill`). Good.

**Schema**: Creates new tables (`bookmark_embeddings`, `bookmark_vec`, fts) — these are derived/additive, not edits to frozen Prisma schema. Borderline but acceptable as separate derived tables.

**loadExtension security**: extension path from env, `existsSync` checked. No injection. No execFile here.

Let me note the dimension/model-mixing as the main correctness blocker.

```json
{"verdict":"APPROVE_WITH_CHANGES","blockers":[],"high":[{"id":"H1","file":"src/lib/vec.ts","line_hint":"searchBruteForce: SELECT bookmark_id, vector_json FROM bookmark_embeddings (no model/dimension filter)","issue":"Brute-force search loads ALL embedding rows regardless of model, and cosineSimilarity uses Math.min(a.length,b.length) which silently compares vectors of different dimensions. If the corpus ever contains two models (e.g. you sampled with --limit on text-embedding-3-small, then swapped SIFTLY_EMBED_MODEL to a local model with different dims), search returns garbage-ranked results with no error — directly undermining Search priority A (known-item recall).","why_destructive_or_wrong":"Not destructive but a real-path correctness bug: query vector is embedded with the *current* provider model while stored vectors may be from a *different* model/dimension; mixing them produces meaningless cosine scores. The single-model test fixture cannot catch this.","fix":"Scope both the query and the brute-force scan to a single model: pass the active model into search and add WHERE model = @model, and reject (or skip) candidates whose dimensions != query length instead of truncating via Math.min."},{"id":"H2","file":"src/lib/vec.ts","line_hint":"ensureSqliteVecTable: CREATE VIRTUAL TABLE IF NOT EXISTS bookmark_vec USING vec0(embedding float[${dimensions}])","issue":"The vec0 virtual table dimension is frozen on first creation. If a later embed run uses a model with different dimensions, INSERT into bookmark_vec fails and the store silently demotes to brute-force (which itself is broken per H1). The vec table is never dropped/recreated on dimension change.","why_destructive_or_wrong":"Provider-swappable embeddings is an explicit contract requirement; swapping to a differently-dimensioned model corrupts the vec path and silently degrades the primary search mode.","fix":"Store the active dimension/model alongside the table; on dimension mismatch, drop+recreate bookmark_vec (and bookmark_vec_rowids) rather than relying on IF NOT EXISTS, or namespace the vec table per model."}],"medium":[{"id":"M1","file":"src/lib/search/index.ts","line_hint":"hybridSearch: if (options.rebuildFts ?? true) rebuildSearchFts(db)","issue":"Every search call defaults to a full DELETE-and-reinsert of the entire FTS5 table from Bookmark. This is a write on the read path: O(corpus) work and a write transaction per query, which contends with the daily cron/enrich writers and scales terribly.","why_destructive_or_wrong":"Not destructive (FTS is a derived table this tier owns) but a correctness/perf hazard: searching while enrichment writes runs causes lock contention, and rebuilding on every query is wrong design. It also means search mutates the DB, surprising for a read operation.","fix":"Default rebuildFts to false; build/refresh FTS incrementally during embed/enrich or via an explicit index command, and treat search as read-only."},{"id":"M2","file":"src/lib/search/embeddings.ts","line_hint":"createEmbeddingProviderFromEnv: provider 'local' still instantiates OpenAIEmbeddingProvider","issue":"SIFTLY_EMBED_PROVIDER=local maps to the OpenAI SDK client with apiKey placeholder 'local' and only works if a base-url is set; otherwise it silently points at api.openai.com and could incur paid spend under a 'local' label.","why_destructive_or_wrong":"Contract names 'local' as a distinct provider; routing it through the OpenAI client without requiring SIFTLY_EMBED_BASE_URL risks unintended paid API calls when an operator believes they are running locally.","fix":"For provider 'local' (and 'openai-compatible'), require a base URL and fail fast if it resolves to the default OpenAI endpoint."},{"id":"M3","file":"src/lib/vec.ts","line_hint":"upsert catch block re-running embedding INSERT after demoteToFallback","issue":"On a sqlite-vec write failure the whole transaction rolls back, then the embeddings-table upsert is re-run outside a transaction. Logic is correct but duplicated; if the retry also throws it is unguarded.","why_destructive_or_wrong":"Minor robustness/duplication issue, not destructive (upsert is idempotent), but the duplicated SQL is a maintenance trap.","fix":"Factor the embeddings upsert into one prepared helper and wrap the retry path so a second failure surfaces cleanly."}],"notes":"No data-destructive operations against other tiers: all DELETEs target derived tables this worker owns (bookmark_embeddings via ON CONFLICT, bookmark_vec by stable rowid, FTS rebuild), upserts are idempotent on re-run, and the unbounded-backfill guard (--limit / --confirm-full-backfill) is correctly enforced. Schema frozen-ness is respected (new additive derived tables, no Prisma edits). The blocking-grade concerns are correctness, not destruction: brute-force search ignores the model column and compares mixed-dimension vectors via Math.min (H1), and the vec0 table dimension is frozen on first create defeating the provider-swap requirement (H2) — both invisible to the single-model fixture and both degrading Search priority A. Recommend APPROVE_WITH_CHANGES contingent on model-scoping search, handling dimension changes, and making search read-only (M1)."}
```